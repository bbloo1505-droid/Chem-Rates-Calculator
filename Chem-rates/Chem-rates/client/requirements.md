## Packages
framer-motion | Page transitions and smooth micro-interactions
date-fns | Human-readable date formatting for the history view
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes efficiently

## Notes
Assuming standard JSON responses from the provided API contract.
No additional auth required based on the schema.
